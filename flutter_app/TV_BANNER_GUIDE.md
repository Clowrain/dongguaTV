# Android TV Banner 图标制作指南

## 概述
Android TV 需要一个 Banner 图标，用于在 TV Launcher 中显示应用。这个图标的尺寸和设计要求与手机应用图标不同。

## 尺寸要求

- **标准尺寸**: 320x180 dp
- **推荐实际尺寸**: 1280x720 px (4倍密度)
- **最小尺寸**: 640x360 px (2倍密度)
- **纵横比**: 16:9
- **格式**: PNG (带透明通道)

## 设计规范

### 1. 布局指南
```
┌────────────────────────────────────┐
│         安全区域                    │
│  ┌──────────────────────────────┐  │
│  │                              │  │
│  │      应用图标 + 文字         │  │
│  │                              │  │
│  └──────────────────────────────┘  │
│         边距 32px                   │
└────────────────────────────────────┘
```

### 2. 内容要求
- ✅ 使用清晰的应用名称
- ✅ 可以包含应用图标
- ✅ 使用高对比度颜色
- ✅ 背景应与品牌色一致
- ❌ 不要过度拥挤
- ❌ 避免细小文字
- ❌ 不要使用透明背景（虽然支持，但不推荐）

### 3. 颜色建议
- **主色调**: 使用应用的品牌色（东瓜橙 #FF6B35）
- **文字**: 白色或高对比度颜色
- **背景**: 深色或渐变背景

## 为 E视界 创建 Banner

### 方案 1: 简约风格
```
┌────────────────────────────────────┐
│  🎬                                │
│  E视界                    Android TV │
│  流媒体聚合播放器                   │
└────────────────────────────────────┘
背景: 深灰色渐变，主色调点缀
```

### 方案 2: 图标+文字
```
┌────────────────────────────────────┐
│                                    │
│    [播放图标]  E视界                │
│              流媒体聚合              │
└────────────────────────────────────┘
背景: 橙色到深色的渐变
```

## 创建步骤

### 使用 Figma/Sketch/Photoshop

1. **创建画布**
   - 新建 1280x720 px 的画布
   - 设置背景色为品牌色或渐变

2. **添加安全边距**
   - 上下左右各留 32px 边距
   - 重要内容放在中央安全区域

3. **添加图标和文字**
   - 应用图标: 建议 120x120 px
   - 应用名称: 字体大小 80-100 px
   - 副标题: 字体大小 40-50 px

4. **导出**
   - 格式: PNG
   - 压缩: 优化但保持质量
   - 命名: `banner.png`

### 使用代码生成（临时方案）

如果暂时没有设计资源，可以使用简单的纯色背景：

```dart
// 这是一个简化的示例，实际应该用图像编辑软件制作
import 'dart:ui' as ui;
import 'package:flutter/material.dart';

Future<void> createBanner() async {
  final recorder = ui.PictureRecorder();
  final canvas = Canvas(recorder);

  // 背景
  final paint = Paint()..color = Color(0xFF1E1E1E);
  canvas.drawRect(Rect.fromLTWH(0, 0, 1280, 720), paint);

  // 这里可以添加更多绘制逻辑...

  final picture = recorder.endRecording();
  final image = await picture.toImage(1280, 720);
  // 保存图像...
}
```

## 安装 Banner

### 1. 准备文件
将制作好的 Banner 放在以下位置：
```
flutter_app/android/app/src/main/res/
├── drawable-xhdpi/banner.png   (640x360)
├── drawable-xxhdpi/banner.png  (960x540)
└── drawable-xxxhdpi/banner.png (1280x720)
```

### 2. 更新 AndroidManifest.xml
Banner 已经配置在 manifest 中：
```xml
<application
    android:banner="@mipmap/ic_launcher"  <!-- 临时使用应用图标 -->
    ...>
```

将其更改为：
```xml
<application
    android:banner="@drawable/banner"
    ...>
```

## 快速方案 - 使用在线工具

### 推荐工具
1. **Canva** (https://www.canva.com)
   - 搜索 "Android TV Banner" 模板
   - 修改文字和颜色
   - 导出为 PNG

2. **Figma** (https://figma.com)
   - 创建 1280x720 画布
   - 使用社区模板
   - 导出为 PNG

3. **Photopea** (https://www.photopea.com)
   - 在线 Photoshop 替代品
   - 完全免费
   - 支持 PSD 格式

## 示例 Banner 配色方案

### 方案 A: 经典深色
- 背景: #1E1E1E → #0A0A0A (渐变)
- 主色: #FF6B35 (东瓜橙)
- 文字: #FFFFFF

### 方案 B: 活力橙色
- 背景: #FF6B35 → #C23616 (渐变)
- 强调: #FFD700 (金色)
- 文字: #FFFFFF

### 方案 C: 现代极简
- 背景: #2C3E50 (深蓝灰)
- 强调: #FF6B35 (东瓜橙)
- 文字: #ECF0F1 (浅灰)

## 测试 Banner

1. 构建并安装 APK 到 Android TV
2. 打开 TV Launcher
3. 查看应用图标是否正确显示
4. 确认 Banner 清晰可读

## 注意事项

⚠️ **重要提示**:
- Banner 必须清晰，因为用户会从 3 米外观看（10-foot UI）
- 避免使用细小的图标或文字
- 确保在不同背景下都清晰可见
- 测试在电视上的实际显示效果

## 下一步

1. 使用上述工具创建 Banner 图标
2. 将文件放入 `android/app/src/main/res/drawable-xxxhdpi/` 目录
3. 更新 `AndroidManifest.xml` 中的 banner 引用
4. 重新构建 APK: `flutter build apk`
5. 在 Android TV 上测试

## 参考资源

- [Android TV 设计规范](https://developer.android.com/design/ui/tv)
- [TV Banner 最佳实践](https://developer.android.com/training/tv/start/start#banner)
- [Material Design for TV](https://material.io/design/platform-guidance/android-tv.html)
